# Program Synthesis (Animal Classification)

## How to install

**Windows or Linux**:

```bash
pdm install --dev -G cuda
```

**Mac**:

```bash
# TODO: You don't need the pytorch-cuda index, but I have no idea how to configure that*
```
